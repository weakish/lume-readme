Run `deno fmt && deno lint && deno test` before committing changes.

If you are using AI assistance:

- Do not set AI agents or LLM models as authors or co-authors. Add an
  `Assisted-by: AGENT_NAME:MODEL_VERSION` line to the commit message instead.
- Reviewing all AI-generated code before opening a pull request.
- You take full responsibility for the contribution.
